# 使用指南

```
 pip install modal
 python -m modal setup
 cd qwen3.5-triton
 modal run run_on_modal.py --which recurrent   
 modal run run_on_modal.py --which compare_fla  
 
 
```

需要添加其他测试可以在qwen3.5-triton\run_on_modal.py中添加